-- Git: hunks in the sign column, hunk actions and blame (gitsigns).
-- The full git client arrives with task 13.

local spec = require("settings").spec

return {
  spec("lewis6991/gitsigns.nvim", "gitsigns"),
}
